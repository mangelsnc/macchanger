# MAC Changer

🔄 MAC Changer

## Usage

```shell
❯ ./macchanger.py -h
usage: macchanger [-h] -i IFACE -m NEW_ADDRESS

MAC address changer

options:
  -h, --help            show this help message and exit
  -i IFACE, --iface IFACE
                        Interface to change the MAC address. Ex: -i eth0 } -iface eth0
  -m NEW_ADDRESS, --mac NEW_ADDRESS
                        New MAC address. Ex: -m CA:FE:CA:FE:CA:FE | --mac
                        CA:FE:CA:FE:CA:FE
```
